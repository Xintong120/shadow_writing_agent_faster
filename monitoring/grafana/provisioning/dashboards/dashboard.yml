apiVersion: 1

providers:
  - name: "shadow-writing-dashboard"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
